namespace PrismExtensionServices.Shared;

/// <summary>
/// document.receipt_type — the transaction category on a posted document.
/// </summary>
public enum ReceiptType
{
    Sale    = 0,
    Return  = 1,
    Deposit = 2,
}
